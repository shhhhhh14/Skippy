class Die
  attr_accessor :side
  def initialize
    @side
  end
  def throw
    random == Random.rand(1..4)
    case random
    when rand(1)
      @side = :North

    when rand(2)
      @side = :East

    when rand(3)
      @side = :South

    when
      @side = :West
    end
  end
end
